/// Defines the type of file to pick.
enum FilePickerType {
  /// Pick an image using the device camera.
  camera,

  /// Pick an image from the gallery.
  gallery,

  /// Pick a PDF document.
  pdf,

  /// Pick a video from the gallery.
  video,
}
